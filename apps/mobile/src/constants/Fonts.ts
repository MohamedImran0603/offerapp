export const Fonts = {
  // Main font for body text and most UI elements
  main: 'Poppins', // fallback to system sans-serif if not loaded
  // Premium font for headings, special UI sections
  premium: 'Satoshi',
  // Clean font for secondary text, inputs, etc.
  clean: 'Inter',
};
